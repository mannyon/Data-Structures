#include <iostream>
#include <string.h>
#include <vector>
#include <string>

using namespace std;

int main () {

     vector<string> str = {"MMM", "PGM", "GP"};
     vector<int> travel ({3,10});
     string mystr;

    int truckM=0, truckP=0, truckG=0;
    for (int i = 0; i < str.size(); ++i) {
        cout << "Index: " << i << ", Value: " << str[i] << endl;
        // mystr.push_back(str[i]);
    }









    // string key = "the quicky brown fox";
    //  cout<<vectorString;
    // char start = 'a';
    // char mapping [280] = {0};
    // char ch = 't';
    // mapping[ch]='a';
    // cout<<mapping;

}